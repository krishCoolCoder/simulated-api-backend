{
  "name": "simulated-api-backend",
  "version": "1.0.0",
  "description": "This the initial stage of the simulated api back-end app.",
  "main": "index.js",
  "scripts": {
    "run":"tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
